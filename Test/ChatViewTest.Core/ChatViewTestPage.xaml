<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns:chatview="clr-namespace:ChatView.Shared.Views;assembly=ChatView.Shared"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
    x:Class="ChatViewTest.Core.ChatViewTestPage">
	<ContentPage.Content>
        <StackLayout >
            <Button Command="{Binding AddCommand}" Text="Click"/>
            <chatview:MessageListView 
                x:Name="OfList"
                ItemsSource="{Binding List}"
                VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand">
               <!-- <chatview:MessageListView.ItemTemplate>
                    <DataTemplate>
                        <chatview:ImageMessageCell
                            ImageUri="{Binding ImageUri}"
                            ImageByteArray="{Binding ImageByteArray}"
                            ImageLoadCallback="{Binding ImageLoadCallback}"
                            CornerRadius="16"
                            Placeholder="ph.png">
                            
                        </chatview:ImageMessageCell>
                    </DataTemplate>
                </chatview:MessageListView.ItemTemplate>-->
            </chatview:MessageListView>
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
